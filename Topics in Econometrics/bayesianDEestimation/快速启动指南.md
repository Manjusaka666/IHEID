# 快速启动指南 (Quick Start Guide)

## 📋 前置检查清单

在开始之前，确认以下项目：

- [ ] 已安装 **R (>= 4.0.0)**
- [ ] 已安装 **Julia (>= 1.9.0)** 并添加到PATH
- [ ] 已安装 **RStudio** (可选但推荐)
- [ ] 已注册 **FRED API key**: https://fred.stlouisfed.org/docs/api/api_key.html
- [ ] 电脑有 **至少16GB RAM** (推荐32GB)
- [ ] 硬盘有 **至少5GB可用空间**

## 🚀 30秒快速启动

### 1. 设置API密钥（仅首次）

**Windows命令提示符:**
```cmd
setx FRED_API_KEY "你的API密钥粘贴在这里"
```

**重启命令行窗口** 使环境变量生效

### 2. 运行分析

```bash
# 进入项目目录
cd "e:\IHEID Economics\IHEID\Topics in Econometrics\bayesianDEestimation"

# 一键运行全部分析
Rscript main_analysis.R
```

**就这么简单！** 脚本会自动：
- ✅ 安装缺失的R包
- ✅ 初始化Julia环境
- ✅ 下载FRED数据
- ✅ 运行所有分析阶段
- ✅ 生成结果表格

## ⏱️ 预计运行时间

| 阶段 | 时间 | 说明 |
|------|------|------|
| 环境设置 | 2分钟 | 自动安装依赖 |
| 数据下载 | 5分钟 | 从FRED API获取 |
| 数据转换 | 1分钟 | Julia加速处理 |
| BVAR测试 | 3分钟 | 验证配置 |
| **递归预测** | **35-60分钟** | ⚠️ 最耗时，使用26线程 |
| 预测评估 | 5分钟 | RMSFE计算 |
| CG回归 | 3分钟 | 行为偏差分析 |
| **总计** | **55-80分钟** | |

## 📊 预期输出

成功运行后，你将获得：

### 关键结果文件

```
results/tables/
├── rmsfe_results.csv              ← RMSFE表格（用于论文Table 1）
├── relative_rmsfe.csv             ← 相对表现（BVAR vs RW）
└── cg_regression_results.csv      ← CG回归系数（核心发现）
```

### 数据文件

```
results/forecasts/
├── small_model_forecasts.rds      ← 230个预测点 × 4变量
├── medium_model_forecasts.rds     ← 230个预测点 × 6变量
├── full_model_forecasts.rds       ← 230个预测点 × 7变量
├── aligned_forecasts_actuals.rds  ← 预测-实际对齐数据
└── hyperparameters_evolution.csv  ← λ演化（可视化用）
```

## 🎯 核心研究问题

### 问题1: 扩展信息集是否改进预测？

**查看结果:**
```bash
type results\tables\relative_rmsfe.csv
```

**解读:**
- **Relative RMSFE < 1**: BVAR优于Random Walk ✅
- **Relative RMSFE > 1**: Random Walk更好 ❌

**预期发现:** Medium/Full模型在短期预测(h=1,3)表现更好

### 问题2: 情绪是否导致诊断性过度反应？

**查看结果:**
```bash
type results\tables\cg_regression_results.csv
```

**解读CG系数β:**
- **β = 0**: 理性预期（无偏差）
- **β > 0**: 反应不足（粘性信息）
- **β < 0**: 过度反应（诊断性预期）✨

**关键假设:** 添加情绪后，β应向负值移动

## 🔧 故障排除

### 错误: "Julia not found"

```bash
# 检查Julia是否安装
julia --version

# 如果未找到，下载安装: https://julialang.org/downloads/
# 然后设置环境变量
setx JULIA_HOME "C:\Users\你的用户名\AppData\Local\Programs\Julia-1.10.0\bin"
```

### 错误: "FRED_API_KEY not set"

```bash
# 注册免费API key: https://fred.stlouisfed.org/docs/api/api_key.html
setx FRED_API_KEY "你的密钥"

# 重启命令行窗口
```

### Phase 5运行超过2小时

检查是否使用了并行计算：

1. 打开任务管理器 (Ctrl+Shift+Esc)
2. 应该看到约26个 `Rscript.exe` 进程
3. CPU使用率应接近100%

如果只有1个进程：
```r
# 检查并行设置
options(bayesian_de.n_cores = 26)
```

### 内存不足错误

**临时解决方案:**
```r
# 编辑 code/05_recursive_forecasting.R
# 第14-15行改为:
n_draw = 5000   # 从10000减少
n_burn = 2500   # 从5000减少
```

## 📈 结果验证

### 快速检查结果是否合理

```r
# 在R中运行
library(tidyverse)

# 检查RMSFE
rmsfe <- read.csv("results/tables/rmsfe_results.csv")
print(rmsfe)

# CPI通胀RMSFE应该在1-3之间（合理范围）
# INDPRO增长RMSFE应该在2-5之间

# 检查CG系数
cg <- read.csv("results/tables/cg_regression_results.csv")
print(cg)

# β系数应该在-1到+1之间（合理范围）
# 如果|β| > 2，可能有问题
```

## 📝 集成到论文

### 步骤1: 导入LaTeX表格

```latex
% 在 ForecastingHorseRace.tex 中

% RMSFE表格
\begin{table}[htbp]
\caption{Forecast Accuracy: RMSFE Comparison}
\label{tab:rmsfe}
\input{results/tables/rmsfe_latex.tex}  % 待生成
\end{table}

% CG回归表格
\begin{table}[htbp]
\caption{Coibion-Gorodnichenko Regression Results}
\label{tab:cg}
\input{results/tables/cg_latex.tex}  % 待生成
\end{table}
```

### 步骤2: 解读结果

在论文中回答：

1. **Medium模型是否优于Small?**
   - 检查 relative_rmsfe.csv
   - 运行DM检验查看统计显著性

2. **Full模型的β系数是否更负?**
   - 计算 Δβ = β_Full - β_Small
   - 如果 Δβ < 0 且显著，支持诊断性预期假设

3. **在哪些变量/时域上发现效应最强?**
   - CPI vs INDPRO
   - h=1 vs h=3 vs h=12

## 🎓 下一步

1. **仔细阅读输出:**
   - `results/tables/` 中的所有CSV文件
   - 计算 Δβ 并检验显著性

2. **创建可视化:**
   - 时间序列图：预测vs实际
   - CG散点图：误差 vs 修正
   - λ演化图：显示自适应shrinkage

3. **撰写论文:**
   - 描述方法论（参考 `bayesianDEestimation.tex` 大纲）
   - 报告RMSFE结果
   - 解释CG系数的经济含义

4. **稳健性检验:**
   - 不同初始窗口大小
   - 不同lag长度 (p=6 vs p=12)
   - 替代情绪指标

## 📚 额外资源

- **详细文档:** [README.md](README.md)
- **命令行指南:** [命令行执行指南.md](命令行执行指南.md)

## 💡 专业提示

1. **首次运行建议在晚上/周末:** Phase 5需要35-60分钟不间断运行

2. **监控进度:**
   ```bash
   # 检查checkpoint文件数量（每50个预测点生成一个）
   dir /b results\forecasts\checkpoints | find /c ".rds"
   # 应该最终有9个文件（3个模型 × 3个checkpoint）
   ```

3. **性能优化:**
   - 临时关闭杀毒软件实时扫描
   - 设置Windows电源计划为"高性能"
   - 确保电脑不会进入睡眠模式

4. **保存中间结果:**
   - 所有checkpoint文件会自动保存
   - 如果中断，重新运行会从checkpoint恢复

---

**问题反馈:** 如遇到未解决的问题，检查:
1. R控制台的错误信息
2. `results/analysis_log_*.txt` 日志文件
3. 各脚本开头的注释说明

**祝研究顺利！🎉**
