# INTERNAL_MAPPING (Manuscript ↔ Outputs)

This file maps every Table/Figure item cited in the submission main text (and any explicitly cited appendix items) to its exact, pre-existing source under `results/`.

| Manuscript item | Source file(s) in repo |
|---|---|
| Table `\ref{tab:rmsfe}` (RMSFE horse race) | `results/latex_tables/tab_rmsfe.tex`; `results/tables/rmsfe_results.csv` |
| Figure `\ref{fig:rmsfe_bar}` (RMSFE bar chart) | `results/figures/fig1_rmsfe_comparison.png`; `results/tables/rmsfe_results.csv` |
| Table `\ref{tab:cg_regression}` (revision diagnostic / CG regression) | `results/latex_tables/tab_cg_regression.tex`; `results/tables/cg_regression_results.csv` |
| Appendix Table `\ref{tab:clark_west}` (nested-robust forecast test) | `results/latex_tables/clark_west_tests.tex`; `results/tables/clark_west_tests.csv` |

## Numerical Integrity Rule

All quantitative values in the paper are contained exclusively in the referenced tables/figures generated by the existing pipeline; the prose contains no standalone numeric claims. Any quantitative statement in the prose must point to a mapped item.

